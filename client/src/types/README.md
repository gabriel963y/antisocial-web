# types

Tipos e interfaces de TypeScript compartidos.

**Ejemplo:**
- `user.ts`:
  ```ts
  export interface User {
    _id: string
    nickName: string
    email: string
    password?: string
    createdAt?: string
  }
  ```
