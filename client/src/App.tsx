import './App.css';

function App() {

    /*
     * 1. pnpm run docker:up          (levanta MongoDB)
     * 2. pnpm install                (instala dependencias)
     * 3. Crear .env (copiar .env.example)
     * 4. pnpm run dev                (levanta backend + frontend juntos)
     * 5. http://localhost:3000/api-docs (swagger)
     * 6. http://localhost:5173          (frontend)
     * 7. pnpm run docker:down        (para MongoDB)
     * */
    return (
        <>
        </>
    );
}

export default App;
