# features/users

Listado y búsqueda de usuarios (funcionalidad extra).

**Qué crear acá (opcional):**
- `UserList.tsx` — `GET /users` → muestra tarjetas con todos los usuarios
- `UserCard.tsx` — componente de tarjeta con nickName y link al perfil
