# components

Componentes reutilizables de UI que no pertenecen a una feature específica.

**Subcarpetas:**
- `layout/` — estructura general (Header, Footer, MainLayout)
- `ui/` — componentes atómicos (Button, Input, Card, ProtectedRoute, etc.)
